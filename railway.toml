[build]
builder = "NIXPACKS"
buildCommand = "cd portal-backend && npm ci && npx prisma generate && npm run build"

[deploy]
startCommand = "cd portal-backend && npx prisma migrate deploy && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
