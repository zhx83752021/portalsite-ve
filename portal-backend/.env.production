# ================================
# 生产环境配置文件
# 警告：请勿提交包含真实密钥的文件到版本控制
# ================================

# 应用配置
NODE_ENV=production
PORT=3000

# 数据库配置 (PostgreSQL)
# 格式: postgresql://用户名:密码@主机:端口/数据库名?schema=public
# 示例: postgresql://user:pass@host.com:5432/dbname?schema=public
DATABASE_URL="postgresql://postgres:YOUR_SECURE_PASSWORD@your-db-host.com:5432/portal_db?schema=public"

# JWT配置
# 重要：请生成一个强密钥！可使用: openssl rand -base64 64
JWT_SECRET=YOUR_SUPER_SECRET_JWT_KEY_CHANGE_THIS_IN_PRODUCTION
JWT_EXPIRES_IN=7d

# CORS配置
# 允许的前端域名（多个域名用逗号分隔）
CORS_ORIGIN=https://your-domain.com,https://www.your-domain.com

# 速率限制配置
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# BCrypt加密强度（10-12推荐）
BCRYPT_ROUNDS=10

# 日志级别 (error, warn, info, debug)
LOG_LEVEL=info
